# Environment Variables

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/genai_saas

# OpenAI
OPENAI_API_KEY=sk-...

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Magento (optional, per merchant)
# MAGENTO_API_URL=https://merchant-store.com
# MAGENTO_API_TOKEN=...

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment
ENVIRONMENT=development

# ngrok (optional - for tunneling localhost to a public URL)
# Paste your token here (do NOT commit .env). Get it from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=
# Optional: full path to ngrok.exe if it's not on PATH (example: C:\Tools\ngrok\ngrok.exe)
NGROK_EXE=
NGROK_BACKEND_PORT=8000
NGROK_FRONTEND_PORT=5173

# Local HTTPS (optional)
SSL_CERTFILE=
SSL_KEYFILE=
